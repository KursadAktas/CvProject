@model List<Hobby>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
<br />
<h2>Hobi Listesi</h2>

<table class="table table-bordered">

    <tr>
        <th>Id</th>
        <th>Hobi</th>
        <th>Hobi 2</th>
        <th>Güncelle</th>
        <th>Sil</th>

    </tr>
    @foreach (var item in Model)
    {

        <tr>
            <td>@item.Id</td>
            <td>@item.Description1</td>
            <td>@item.Description2</td>
            <td>
                <a class="btn btn-info btn-sm" asp-controller="Hobby" asp-action="Update" asp-route-id="@item.Id">
                    <i class="fas fa-pencil-alt">
                    </i>
                    Güncelle
                </a>
            </td>
            <td>
                <a asp-route-id="@item.Id" asp-controller="Hobby" asp-action="Delete" class="btn btn-danger btn-sm">
                    <i class="fas fa-trash">
                    </i>
                    Sil
                </a>
            </td>

        </tr>
    }

</table>

<a asp-controller="Hobby" asp-action="Add" class="btn btn-primary">Yeni Hobi Ekle</a>